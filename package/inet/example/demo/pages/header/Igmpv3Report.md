# IGMPv3 membership report — RFC 3376

A list whose elements each decide their own length: a group record carries a
source list of its own, so no arithmetic over a count can find where the next
record starts. The list fills its window instead, and the count beside it
derives from what is in it.

```pred-ref
<<header_view("Igmpv3Report")>>
```
