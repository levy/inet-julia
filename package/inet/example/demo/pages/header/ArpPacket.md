# ARP — RFC 826

Four constant fields that hold nothing and still take width, and two address
families in one header: MAC and IPv4, each printing as the standard writes it.

```pred-ref
<<header_view("ArpPacket")>>
```
