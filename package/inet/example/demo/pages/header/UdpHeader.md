# UDP — RFC 768

The smallest header there is. Two fields a caller states, two the declaration
gives a default — and the two with defaults are exactly the two a datagram
cannot know on its own.

```pred-ref
<<header_view("UdpHeader")>>
```
