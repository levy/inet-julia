# IPv4 — RFC 791

The datagram header every network still runs on: twenty octets, four of them
sub-byte fields, and an option list that runs to wherever `ihl` says the header
ends.

```pred-ref
<<header_view("Ipv4Header")>>
```
