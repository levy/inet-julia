# Generated simulation + benchmark output (vectors, CSVs, plots).
results/

# Julia resolved dependency lockfiles (each environment resolves its own).
Manifest.toml

# The executable bundle that tool/build_binary.jl produces.
build/

# The workload level is a person's choice, not the project's.
LocalPreferences.toml
